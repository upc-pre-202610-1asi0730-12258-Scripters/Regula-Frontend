## Legacy code

This folder contains older UI/modules that are **not part of the current DDD-based app** (`src/domain`, `src/application`, `src/infrastructure`, `src/presentation`).

They were kept to avoid losing work during the merge, but they are **not wired** to the active `src/main.ts` + `src/presentation/router`.

