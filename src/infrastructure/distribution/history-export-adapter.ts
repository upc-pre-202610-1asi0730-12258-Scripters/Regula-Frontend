/**
 * Stub for queued exports; replace with REST call when backend is available.
 */
export async function queueDeliveryHistoryExport(): Promise<void> {
  await new Promise((resolve) => setTimeout(resolve, 350))
}
