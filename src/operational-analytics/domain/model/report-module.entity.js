export class ReportModule {

    constructor({ key, icon, label, description }) {
        this.key         = key
        this.icon        = icon
        this.label       = label
        this.description = description
    }
}